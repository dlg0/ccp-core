# CCP Core — suggested starting tasks

## Highest priority

1. Freeze v0.1 object IDs, required fields, and status enums
2. Tighten schema definitions and add fixture coverage
3. Implement `ccp validate` against schemas and repo layout
4. Implement `ccp init` control-plane scaffolding
5. Implement `ccp new` with templates
6. Implement `ccp index` summary generation
7. Write conformance profile docs

## Next

8. Add markdown rendering helpers for concise review summaries
9. Add extension mechanism/profile notes
10. Add optional JSON/YAML discussion and canonical-format guidance
11. Add event log semantics
12. Publish first public pre-release tag

## Avoid for now

- building a web UI
- inventing a hosted workflow service
- embedding Foundry product assumptions
